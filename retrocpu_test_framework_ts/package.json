{
  "name": "retrocpu-test-framework",
  "private": true,
  "version": "0.1.0",
  "description": "MN1613 アセンブラを HEX/CDB 経由でエミュレータ検証するテストフレームワーク",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "npm run build:asm && tsx src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build:asm": "npm --prefix ../retrocpu_asm_ts run build"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "tsx": "^4.19.0",
    "typescript": "^5.9.2"
  }
}
