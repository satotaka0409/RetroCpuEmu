{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "msedge",
      "name": "レトロマイコンエミュ Debug",
      "request": "launch",
      "url": "http://127.0.0.1:5173",
      "webRoot": "${workspaceFolder}/src/main",
      "preLaunchTask": "vite:dev",
      "sourceMapPathOverrides": {
        "/@fs${workspaceFolder}/*": "${workspaceFolder}/*",
        "/src/*": "${workspaceFolder}/src/*"
      }
    }
  ]
}
