---
alwaysApply: true
---

# レトロCPU用のBIOS設計書

MN1613 用 BIOS（モニター領域 `0108h`–`17FFh`）の公開エントリ仕様。  
根拠: `HandShake.mdc` / `MN1613_CPUボードメモリ_IOマップ.mdc` / `handshake_common.asm`

注記: テスト時のみ有効な CPU 実行ログ（出力先設定、未指定時は無効、通常実行では無効）は `asm_test_framework.mdc` の「テスト専用 CPU ログ出力」を参照。

## 共通規約

| 項目         | 内容                                                                                                                    |
| :----------- | :---------------------------------------------------------------------------------------------------------------------- |
| 呼出         | `BALD` / `B`（リンクレジスタ規約に従う）                                                                                |
| 引数         | **第1=`R0` / 第2=`R1` / 第3=`R2` / 第4引数以降はスタック**（`asm-rules.mdc`） |
| スタック引数 | 呼び出された側から見て `SP+1` が `BALD` の戻りアドレス、`SP+2` が第4引数。積んだ引数は呼び出し側が `POP` で捨てる |
| 戻り値       | `R0` / `R1` / `R2`（必要な分。ステータスは多くの場合 `R0`）                                                              |
| 成功／失敗   | `R0` に `HSHK_OK (00h)` / `HSHK_NG (01h)`。コマンドにより `02h`（モードエラー等）もあり                                 |
| レジスタ破壊 | **`R0` / `R1` / `R2` は破壊可（caller-saved・戻り可）**。`R3`–`R4` のみ呼ばれた側が先頭で `PUSH` し復帰前に `POP` して保つ |
| バイト順     | 16bit 値は **ビッグエンディアン**（先に上位バイト）でハンドシェイク送信                                                 |
| 前提         | CPU→IO は `g_hshk_initiate_send` → `g_hshk_send_byte`×N → `g_hshk_finalize_send` →（応答があれば IO→CPU 転送で受信） |
| モード       | **フリーモード (`10h`=1)** が必要なコマンドあり。BEEP・未定義命令LED（`13h`）はモード不問 |

> 大きなペイロード（例: `g_hshk_get_time` の 4 ワード）は呼び出し元確保のスタックを使う。**3 ワード以下は R0–R2 を優先**（`g_bios_pc_key_get`、`g_malloc2` の R0+R1 など）。

### 領域と初期化

コードは `.area _CODE (REL,CON)`（`0108h`–、ROM）、値あり定数・テーブルは `.area _DATA (REL,CON)`（ROM、`.word` / `.dw`）、
モニタの作業変数は `.area _WORK (REL,NOLOAD)`（RAM、`.ds`）に置く。
システムゼロページは `.area _SYS_PAGE0 (REL,NOLOAD)`（sdld `-b` がバイト `0010h`＝ワード `0008h` から CON。各 `.asm` は `.ds` のみ、`.org` 不要。`L`/`ST *label`）。
ユーザゼロページは `.area _USR_PAGE0 (REL,NOLOAD)`（`-b` バイト `0080h`＝ワード `0040h`）。
リンクは `sdld`（`-b` と `.lnk`）。CON 順は `_CODE` → `_DATA` → `_WORK`（`_VECTOR` / ゼロページがあればその前）。
sdld は `s__WORK` / `l__WORK` を出す。CDB はバイト、命令即値はワード（÷2）。
割り込みベクタは `.area _VECTOR (ABS,OVR)`（`0100h`）。配置表は
`retrocpu_boot_monitor/mn1613/src/memmap.inc`。`MON_CODE_BASE` / `MON_DATA_BASE`
は使わない。`_WORK` / ゼロページは初期値を持てないため、リセット後に初期化ルーチンを 1 回呼ぶ。
**コード（BIOS／ユーザプログラム）はセグメント 0。** 64k ワード超（SBR `4/8/C`）はデータ専用（`malloc2` 等）。

### `g_rnd_init` — 乱数初期化

実装: `retrocpu_boot_monitor/mn1613/src/bios/bios_common.asm`

|          |                                                                                                                                                                             |
| :------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **説明** | `_SYS_PAGE0` の種 `GL_RND_SEED`（`bios_common.asm` の `.ds 1`、`ST *GL_RND_SEED`）を設定する。`R0=0` は M系列がロックするので `1` にする。リセット後 1 回呼ぶ（`main.asm`） |
| **入力** | `R0` = 種（16bit）                                                                                                                                                          |
| **出力** | なし                                                                                                                                                                        |
| **破壊** | R0                                                                                                                                                                          |

### `g_get_rnd` — 乱数取得

|          |                                                                                                                      |
| :------- | :------------------------------------------------------------------------------------------------------------------- |
| **説明** | 16bit M系列（Galois LFSR）。原始多項式 `x^16+x^14+x^13+x^11+1`（タップ `0xB400`）。周期 `2^16-1`。値は `1`〜`0xFFFF` |
| **入力** | なし                                                                                                                 |
| **出力** | `R0` = 乱数値                                                                                                        |
| **破壊** | R0                                                                                                                   |

### `g_mem_cpy` — メモリコピー

実装: `retrocpu_boot_monitor/mn1613/src/bios/bios_common.asm`

|          |                                                                                                                                                            |
| :------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **説明** | `BLK` でワード列をコピーする。元は `TSR0`+論理、先は `TSR1`+論理。A16–A17 は物理ワードの上位 2bit（`0–3`。TSR は `<<2` で `0/4/8/C`）。語数 0 は何もしない |
| **入力** | `R0` = 元 A16–A17。`R1` = 元 A0–A15。`R2` = 語数。`SP+2` = 先 A16–A17。`SP+3` = 先 A0–A15                                                                   |
| **出力** | なし                                                                                                                                                       |
| **破壊** | R0, R1, R2（R3–R4・TSR0・TSR1 は退避）                                                                                                                     |

```asm
	ld	R0, SRC_A17		; 0–3
	ld	R1, SRC_LOG
	ld	R2, NWORDS
	push	DST_LOG
	push	DST_A17
	bald	g_mem_cpy
	ai	SP, #2
```

### `g_malloc_init` — ヒープ初期化

実装: `retrocpu_boot_monitor/mn1613/src/bios/bios_common.asm`

|          |                                                                                                                                                                               |
| :------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **説明** | first-fit ヒープ。`GL_ALLOC_ADR` / `GL_ALLOC_SIZE`（`bios_common.asm` の `.ds 1`）に範囲を書き、先頭に空きブロックヘッダ（サイズ＋フラグ 0）を置く。サイズ 2 未満はヘッダ無し |
| **入力** | `R0` = 先頭ワードアドレス。`R1` = サイズ（ワード数。ヘッダ 2 ワードを含む）                                                                                                   |
| **出力** | なし                                                                                                                                                                          |
| **破壊** | なし（R0–R2 は破壊可。R3–R4 は退避）                                                                                                                                                          |

### `g_malloc` — ヒープ確保

|          |                                                                                                                                                                            |
| :------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **説明** | 空きブロックを first-fit。各ブロック先頭 2 ワードがヘッダ（サイズ／0=free・1=used）。戻りはユーザ先頭（ヘッダの次）。余りが 2 ワード以上なら分割。不足・サイズ 0 は `R0=0` |
| **入力** | `R0` = 欲しいワード数（1 以上。ヘッダは含まない）                                                                                                                          |
| **出力** | `R0` = ユーザ先頭ワードアドレス。失敗は `0`                                                                                                                                |
| **破壊** | R0, R1, R2（R3–R4 は退避）                                                                                                                                                     |

### `g_free` — ヒープ解放

|          |                                                                                                    |
| :------- | :------------------------------------------------------------------------------------------------- |
| **説明** | `malloc` の戻りを返す。使用中ブロックだけ受け付け、隣接 free を結合する。0・未登録・二重解放は失敗 |
| **入力** | `R0` = `g_malloc` の戻り値                                                                        |
| **出力** | `R0` = 成功時は同じアドレス。失敗は `0`                                                            |
| **破壊** | R0, R1, R2（R3–R4 は退避）                                                                             |

### `g_malloc2_init` — セグメント付きヒープ初期化

実装: `retrocpu_boot_monitor/mn1613/src/bios/bios_common.asm`

|          |                                                                                                                                                                                                               |
| :------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **説明** | first-fit ヒープを指定 SBR 上に置く。`GL_ALLOC2_ADR` / `GL_ALLOC2_SBR` / `GL_ALLOC2_SIZE` に書き、先頭に空きヘッダを置く。サイズ 2 未満はヘッダ無し。ヒープ本体は `TSR0`+`LR`/`STR` で触る（TSR0 は復帰する）。SBR は下位 2bit=0 固定（`andi #0x000C`。既定 `GL_ALLOC2_DEFAULT_SBR=4` → 物理 `10000h–`） |
| **入力** | `R0` = 先頭論理ワードアドレス。`R1` = SBR（有効値 `0/4/8/C`。他はマスク）。`R2` = サイズ（ワード数。ヘッダ 2 ワードを含む）                                                                                                                                              |
| **出力** | なし                                                                                                                                                                                                                                                                                          |
| **破壊** | なし（R0–R2 は破壊可。R3–R4 は退避）                                                                                                                                                                                                                                                                          |

```asm
	ld	R0, HEAP_LOG
	ld	R1, HEAP_SBR		; 0/4/8/C
	ld	R2, HEAP_WORDS
	bald	g_malloc2_init
```

### `g_malloc2` — セグメント付きヒープ確保

|          |                                                           |
| :------- | :-------------------------------------------------------- |
| **説明** | `g_malloc` と同じ first-fit。戻りは論理アドレス＋SBR     |
| **入力** | `R0` = 欲しいワード数（1 以上。ヘッダは含まない）         |
| **出力** | `R0` = ユーザ先頭論理アドレス。`R1` = SBR。失敗は両方 `0` |
| **破壊** | R0, R1, R2（R3–R4・TSR0 は退避）                              |

### `g_free2` — セグメント付きヒープ解放

|          |                                                                                        |
| :------- | :------------------------------------------------------------------------------------- |
| **説明** | `g_malloc2` の戻りを返す。SBR 不一致・0・未登録・二重解放は失敗。隣接 free を結合する |
| **入力** | `R0` = `g_malloc2` の戻り（ユーザ先頭）。`R1` = その SBR                              |
| **出力** | `R0` = 成功時は同じアドレス。`R1` = 同じ SBR。失敗は両方 `0`                           |
| **破壊** | R0, R1, R2（R3–R4・TSR0 は退避）                                                           |

---

## ハンドシェイクルーチン

線制御の低レベル API。上位 BIOS（BEEP / LED / キー）はこれらを組み合わせて実装する。

### `g_hshk_initiate_send` — CPU→IO 開始

|          |                                                                    |
| :------- | :----------------------------------------------------------------- |
| **説明** | ENA=0 確認 → DENA=0 → REQ_0=1 → ENA=1 待ち → REQ_0=0。依頼受理まで |
| **入力** | なし                                                               |
| **出力** | `R0` = OK / NG                                                     |
| **破壊** | R0, R1, R2                                                             |

### `g_hshk_send_byte` — CPU→IO 1バイト送信

|          |                                                       |
| :------- | :---------------------------------------------------- |
| **説明** | `HSHK_IN_DATA` に載せ DENA/DACK の 1 バイト転送を行う |
| **入力** | `R0` = 送信バイト（下位 8bit）                        |
| **出力** | `R0` = OK / NG                                        |
| **破壊** | R0, R1, R2                                                |

### `g_hshk_reg_send16` / `g_hshk_send_word` — CPU→IO 16bit 送信（ビッグエンディアン）

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_common.asm`（同一入口）

|          |                                                              |
| :------- | :----------------------------------------------------------- |
| **説明** | 16bit を上位バイト→下位バイトの順で `g_hshk_send_byte` する |
| **入力** | `R0` = 送信ワード（16bit）                                   |
| **出力** | `R0` = OK / NG（最後の 1 バイト）                            |
| **破壊** | R0, R1, R2                                                       |

### `g_hshk_finalize_send` — CPU→IO 完了

|          |                                              |
| :------- | :------------------------------------------- |
| **説明** | IO 側が ENA=0 にするのを待つ（転送完了通知） |
| **入力** | なし                                         |
| **出力** | `R0` = OK / NG                               |
| **破壊** | R0, R1, R2                                       |

### `g_hshk_wait_req1_1` — IO→CPU 依頼待ち（ポーリング）

|          |                                                                                      |
| :------- | :----------------------------------------------------------------------------------- |
| **説明** | `HSHK_REQ_1` が 1 になるまで待つ。割り込みを使わず同期的に応答を受けたい BIOS が使う |
| **入力** | なし                                                                                 |
| **出力** | `R0` = OK / NG（待ち上限超過で NG）                                                  |
| **破壊** | R0, R1, R2                                                                               |

### `g_handshake_interrupt_handler` — IO→CPU 割り込み入口

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_main.asm`

|          |                                                                                                                                                                                                                                                                                                               |
| :------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **説明** | `INT_CAUSE=2`。受理 → コマンド 1 バイト受信 → `0x10`–`0x18` テーブルで分岐 → `finalize`。`g_int2_handler` から `BALD` されるので `RET` で戻る。`10h`/`11h` は `g_hshk_addr_break_set` / `g_hshk_addr_break_clr`。`13h`/`14h` は `g_hshk_read_memory` / `g_hshk_write_memory`。`17h` は `g_hshk_break_hist_get`。`18h` は `g_hshk_break_resume`。旧 `48h`（CPU状態取得）は未実装扱い |
| **入力** | なし（割り込みコンテキスト）                                                                                                                                                                                                                                                                                  |
| **出力** | なし                                                                                                                                                                                                                                                                                                          |
| **破壊** | R0, R1, R2                                                                                                                                                                                                                                                                                                        |

### `g_breakpoint_interrupt_handler` — 比較器ヒット（INT_CAUSE=3）

実装: `retrocpu_boot_monitor/mn1613/src/breakpoint/breakpoint.asm`

|          |                                                                                                                                                                                                                          |
| :------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **説明** | CPLD 比較器ヒット後の INT2 から `BALD` される。`0033` でスロットを読む（上位ビット付きは無効→スルー）。`0034` で前回書込値を `GL_BP_HIT_PREV` に残す。スロット **0–7** は `GL_HSHK_ADDR_BREAK` の ena／値比較／回数を見る。Bit7 履歴かつ比較一致なら 11h 時刻＋エントリを 3F000h（SBR C）へ書く。ステップは比較器を使わない。停止時は **1Ah** を送り `R0=1`。INT2 は `g_main_loop` へ戻る |
| **入力** | なし（割り込みコンテキスト。線上は 11h（履歴時）と 1Ah（停止時））                                                                                                                                                      |
| **出力** | `R0` = 0（継続／LPSW 2）または 1（モニタ HALT）                                                                                                                                                                          |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する。値比較／履歴時は TSR0 を一時変更）                                                                                                                                                   |
| **線上** | 履歴時 送信 1B: `11h` → 受信 9B。停止時 送信 7B: `1Ah`, 区分(0=命令/1=MEM/2=IO), スロット 0–7, addr32 BE → 受信 1B: status                                                                                   |

> 値比較は MEM かつ条件≠0 のときだけ。IO／命令は条件を無視。不一致・無効スロットはスルー（履歴にも書かない）。  
> Bit7 履歴は回数判定の前に記録する。WRITE 以外／命令は前回値 `0000h`。AFTER は監視アドレスの現在値（IO は 0）。  
> `0033=0xFFFF`（未マップ）はスロット 7 と誤認しない。  
> 11h / 1Ah の OK/NG は IO→CPU なので、応答待ちの前に `INTERRUPT_BUSY` を下ろす（BUSY=1 のままでは配送されない）。

### `g_bp_hist_append` — 比較器ヒット履歴 1 件追記

実装: `retrocpu_boot_monitor/mn1613/src/breakpoint/breakpoint_hist.asm`

|          |                                                                                          |
| :------- | :--------------------------------------------------------------------------------------- |
| **説明** | Bit7 履歴かつ値比較一致のあと `g_breakpoint_interrupt_handler` から `BALD`。11h 時刻＋33 語（レジスタ＋スタック 16 ワード）を 3F000h（SBR C）のリングへ書く。失敗時も時刻 0 で書く |
| **入力** | `R2` = 1Ah 区分、`R3` = スロット 0–7、`X1` = 10h 表ポインタ                             |
| **出力** | なし                                                                                     |
| **破壊** | R0, R1（R2 / R3 / X1 は保存。TSR0 は入口スナップへ戻す）                                 |

### `g_hshk_break_resume` — ブレイク復帰実行（コマンド `18h`）

実装: `retrocpu_boot_monitor/mn1613/src/breakpoint/breakpoint_steprun.asm`

|          |                                                                                                                                 |
| :------- | :------------------------------------------------------------------------------------------------------------------------------ |
| **説明** | IO→CPU 転送中に実行方式を受ける。0=通常再開、1=ステップ（`GL_STEP_ARM=1`）。2 以上は NG。ENA はここでは上げない（INT2 の `LPSW 2` 直前の `g_step_arm_cpld`） |
| **入力** | なし（線上 1B: 実行方式）                                                                                                       |
| **出力** | `R0` = OK / NG                                                                                                                  |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                                                            |
| **線上** | 受信 1B → 送信 1B status                                                                                                        |

### `g_step_arm_cpld` — ステップ用 CPLD 武装

実装: `retrocpu_boot_monitor/mn1613/src/breakpoint/breakpoint_steprun.asm`

|          |                                                                                                      |
| :------- | :--------------------------------------------------------------------------------------------------- |
| **説明** | INT2 エピローグが `LPSW 2` の直前に `BALD`。`0037h` へ `2006h`（LPSW 2）を書く。`GL_STEP_ARM≠0` なら `0036h=1` にしてフラグを落とす |
| **入力** | なし                                                                                                 |
| **出力** | なし                                                                                                 |
| **破壊** | R0, R1, R2                                                                                           |

### `g_step_interrupt_handler` — ステップヒット（INT_CAUSE=4）

実装: `retrocpu_boot_monitor/mn1613/src/breakpoint/breakpoint_steprun.asm`

|          |                                                                                                              |
| :------- | :----------------------------------------------------------------------------------------------------------- |
| **説明** | ユーザ 1 命令の実行後に INT2 から `BALD`。比較器スロットは使わない。**1Bh** でアドレス・レジスタ・スタック 16 ワードを送る。履歴リングには書かない。`R0=1` でモニタ HALT |
| **入力** | なし（割り込みコンテキスト）                                                                                 |
| **出力** | `R0` = 1（モニタ HALT）                                                                                      |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                                         |
| **線上** | 送信 59B: `1Bh`, addr32 BE（上位 0、下位=レベル2 IC 退避）, レジスタ（17h と同じ並び）, スタック 16 ワード → 受信 1B status                         |

> ENA=1 はトリガ命令（`LPSW 2`）の直前に限る。通常再開（`18h` 方式 0）は ENA を上げない。

### `g_hshk_addr_break_init` — 比較器ブレイクスロット初期化

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_address_break.asm`

|          |                                                                                                      |
| :------- | :--------------------------------------------------------------------------------------------------- |
| **説明** | `_WORK` の `GL_HSHK_ADDR_BREAK`（**ユーザ 8** スロット × 6 ワード）を 0 にする。リセット後 1 回 |
| **入力** | なし                                                                                                 |
| **出力** | なし                                                                                                 |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                                 |

### `g_hshk_addr_break_set` — 比較器ブレイク設定（コマンド `10h`）

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_address_break.asm`

|          |                                                                                                                                                                                                    |
| :------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **説明** | IO→CPU 転送中（コマンド 1B 受信済み）にスロット **0–7** へ命令／メモリ／IO ブレイクを設定。番号不正は NG。INT3 は使わない。CPLD IO 未マップ時は `_WORK` 表に保持し、実機では CPLD へ書く想定 |
| **入力** | なし（線上 9B: slot, flags, count, addr32 BE, data16 BE）                                                                                                                                          |
| **出力** | `R0` = OK / NG（線上 status と同じ）                                                                                                                                                               |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                                                                                                                               |
| **線上** | 受信 9B → 送信 1B status                                                                                                                                                                           |
| **表**   | 1 スロット 6 ワード: ena / flags / count / addr_hi / addr_lo / data。flags は `HandShake.mdc` 位置02（Bit0 MEM/IO, Bit1 RD_EN, Bit2 WR_EN, Bit3–5 条件, Bit6 INST, Bit7 履歴）                     |

### `g_hshk_addr_break_clr` — 比較器ブレイク解除（コマンド `11h`）

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_address_break.asm`

|          |                                                                                  |
| :------- | :------------------------------------------------------------------------------- |
| **説明** | IO→CPU 転送中にスロット **0–7** をクリア。番号不正は NG、他スロットは変えない |
| **入力** | なし（線上 1B: slot）                                                            |
| **出力** | `R0` = OK / NG                                                                   |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                             |
| **線上** | 受信 1B → 送信 1B status                                                         |

> 旧 `42h`/`43h`（INT3 命令パッチ）は廃止。命令ブレイクは `10h` の MEM+RD_EN（Bit6=INST）。  
> ステップ実行は比較器を使わない（`retrocpu_debug.mdc`）。

### `g_hshk_break_hist_get` — ブレイク履歴取得（コマンド `17h`）

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_break_hist.asm`

|          |                                                                                                                                                                                                 |
| :------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **説明** | IO→CPU 転送中にスロット **0–7** の履歴リング（3F000h、SBR C）を新しい順で返す。番号不正は NG。Bit7 未設定は件数 0 で `02h`。Bit0=1 なら送信後に当該スロットの履歴をクリアする |
| **入力** | なし（線上 2B: slot, flags。Bit0=取得後クリア）                                                                                                                                               |
| **出力** | `R0` = OK / NG / `02h`（履歴モード未設定）                                                                                                                                                      |
| **破壊** | R0, R1, R2（R3–R4・TSR0 は退避・復元する）                                                                                                                                                      |
| **線上** | 受信 2B → 送信 8B ヘッダ（件数, ステータス, flags, count, addr32 BE）＋ エントリ×件数×66B ＋ 終端 1B                                                                                          |

> ヘッダは `HandShake.mdc` 位置 03–0A。エントリは時刻 8B ＋ AFTER/PREV 各 2B ＋ レジスタ（R0–R4 SP STR IC CSBR/SSBR TSR0/TSR1 NPP、NPP 1B ＋パディング 0）＋ スタック 16 ワードで 66B。  
> ステータス Bit0=リングオーバフロー、Bit1=履歴モードでない。

### `g_hshk_read_memory` — メモリ読み出し（コマンド `13h`）

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_read_memory.asm`

|          |                                                                                                                                                                                                                                 |
| :------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **説明** | IO→CPU 転送中（コマンド 1B 受信済み）に、32bit バイトアドレスとバイト数を受け、256B ブロック＋チェックサム（バイト加算の下位 8bit）を CPU→IO で返す。NG なら同一ブロックを再送。端数はパディングしない。IRQ テーブルから `bald` |
| **入力** | なし（線上ヘッダ 8B: addr32 BE + count32 BE）                                                                                                                                                                                   |
| **出力** | `R0` = OK / NG                                                                                                                                                                                                                  |
| **破壊** | R0, R1, R2（R3–R4・TSR0 は退避・復元する）                                                                                                                                                                                          |
| **線上** | 受信 8B → 繰り返し: 送信 blk(≤256)+checksum → 受信 1B status                                                                                                                                                                    |
| **作業** | 状態 7 ワードはスタック上に確保（`_WORK` は使わない）                                                                                                                                                                           |

### `g_hshk_write_memory` — メモリ書き込み（コマンド `14h`）

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_write_memory.asm`

|          |                                                                                                                                                                                                                                     |
| :------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **説明** | IO→CPU 転送中（コマンド 1B 受信済み）に、32bit バイトアドレスとバイト数を受け、256B ブロック＋チェックサムを IO→CPU で受け取る。一致なら RAM に書いて OK、不一致なら NG で再送待ち。端数はパディングしない。IRQ テーブルから `bald` |
| **入力** | なし（線上ヘッダ 8B: addr32 BE + count32 BE、続けてデータ）                                                                                                                                                                         |
| **出力** | `R0` = OK / NG                                                                                                                                                                                                                      |
| **破壊** | R0, R1, R2（R3–R4・TSR0 は退避・復元する）                                                                                                                                                                                              |
| **線上** | 受信 8B → 繰り返し: 受信 blk(≤256)+checksum → 送信 1B status                                                                                                                                                                        |
| **作業** | 状態 8 ワードはスタック。受信ブロック 256 ワードは `g_malloc`（復帰前に `g_free`）                                                                                                                                                |

> バイト↔物理は `g_hshk_mem_map`（`TSR0=phys[17:16]<<2`、論理=`phys[15:0]`。SBR 下位2bit=0）。

### `g_hshk_accept_request` — IO→CPU 依頼受理

|          |                                                                             |
| :------- | :-------------------------------------------------------------------------- |
| **説明** | レベル2割り込み（要因=ハンドシェイク:2）入口。DACK=0 → ENA=1 → REQ_1=0 待ち |
| **入力** | なし（割り込みコンテキスト）                                                |
| **出力** | `R0` = OK / NG                                                              |
| **破壊** | R0, R1, R2                                                                      |

### `g_hshk_recv_byte` — IO→CPU 1バイト受信

|          |                                                                                        |
| :------- | :------------------------------------------------------------------------------------- |
| **説明** | DENA=1 待ち → `HSHK_OUT_DATA` 読取 → DACK ハンドシェイク。リエントラント（作業域なし） |
| **入力** | なし                                                                                   |
| **出力** | `R0` = OK / NG。`R1` = 受信バイト（下位 8bit、OK 時）                                  |
| **破壊** | R0, R1, R2                                                                                 |

### `g_hshk_finalize_recv` — IO→CPU 完了

|          |                                     |
| :------- | :---------------------------------- |
| **説明** | CPU 側で ENA=0 をセットし完了を通知 |
| **入力** | なし                                |
| **出力** | `R0` = OK                           |
| **破壊** | R0, R1, R2                              |

### `g_bios_mode_set` — モード設定（コマンド `10h`）

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_chg_mode.asm`

|          |                                                                                          |
| :------- | :--------------------------------------------------------------------------------------- |
| **説明** | モニター(0) / フリー(1)。LED・16進キーはフリー必須。0/1 以外は IO が NG、設定は変えない |
| **入力** | `R0` = モード（0 または 1）                                                              |
| **出力** | `R0` = OK / NG（応答 1 バイト）                                                          |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                         |
| **線上** | 送信 2B: `10h`, mode → 受信 1B: status                                                   |

> 応答は送信完了後の **IO→CPU 別トランザクション**。`g_hshk_wait_req1_1` で
> `HSHK_REQ_1` をポーリングし、`accept` → `recv_byte` → `finalize_recv`（同期）。

---

## LCD1602

モード不問。7セグ／砲弾（`16h`）とは独立。線上は `HandShake.mdc` の **17h / 18h**。

### `g_bios_lcd_control` — LCD制御（コマンド `17h`）

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_lcd1.asm`

|          |                                                                                          |
| :------- | :--------------------------------------------------------------------------------------- |
| **説明** | Clear / Home / DisplayCtrl / SetCursor。種別以外は NG                                    |
| **入力** | `R0` = 種別（0:Clear 1:Home 2:DisplayCtrl 3:SetCursor）。`R1` = 引数A（DisplayCtrl ビット）。`R2` = 引数B（行 0/1）。`R3` = 引数C（列 0–15） |
| **出力** | `R0` = OK / NG / `02h`                                                                   |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                     |
| **線上** | 送信 5B: `17h`, kind, argA, argB, argC → 受信 1B: status                                 |
| **作業** | 送信フレーム 5 ワードはスタック上に確保（`_WORK` は使わない）                            |

> 第4引数はスタックではなく **`R3`**（実装どおり。呼び出し側は R3 に列を置く）。

### `g_bios_lcd_text` — LCD文字列表示（コマンド `18h`）

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_lcd2.asm`

|          |                                                                                          |
| :------- | :--------------------------------------------------------------------------------------- |
| **説明** | 指定行・列から最大 16 ASCII を書く。len 未満は空白 `20h` で埋める。行末で打ち切る        |
| **入力** | `R0` = 行（0/1）。`R1` = 列（0–15）。`R2` = 文字数（0–16）。`R3` = バッファ先頭（1 ワード 1 バイト） |
| **出力** | `R0` = OK / NG / `02h`                                                                   |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                     |
| **線上** | 送信 20B: `18h`, row, col, len, ch0..ch15 → 受信 1B: status                              |
| **作業** | 送信フレーム 20 ワードはスタック上に確保（`_WORK` は使わない）                           |

> 文字列先頭も **`R3`**（実装どおり）。

---

## BEEP音

ハンドシェイク **コマンド `19h`**。モード不問（モニターでも可）。

### `g_bios_beep`

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_beep.asm`

|          |                                                                              |
| :------- | :--------------------------------------------------------------------------- |
| **説明** | IO ボードのスピーカーで BEEP。`周波数=0` で停止、`長さ=0` で無限。モード不問 |
| **入力** | `R0` = 周波数 Hz（16bit）。`R1` = 長さ ms（16bit）                           |
| **出力** | `R0` = OK / NG                                                               |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                             |
| **線上** | 送信 5B: `19h`, freqH, freqL, durH, durL → 受信 1B: status                   |
| **作業** | 送信フレーム 5 ワードはスタック上に確保（`_WORK` は使わない）                |

| 入力値   | 意味                     |
| :------- | :----------------------- |
| freq = 0 | 鳴動停止                 |
| dur = 0  | 停止指示まで無限に鳴らす |
| dur > 0  | 指定ミリ秒後に自動停止   |

---

## タイマー割り込み

ハンドシェイク **コマンド `12h`（タイマー設定）**。モード不問。
タイマーは **IO ボード側** にあり、番号 **0 / 1** の 2 本。**初期化直後は停止**しており、
このコマンドを出したときだけ動き出す。停止も同じコマンド（周期 0）で行う。

満了すると IO ボードが **レベル2割り込み**を上げ、`INT_CAUSE` に **タイマー番号**が載る
（`INT_CAUSE_TIMER0`=0 / `INT_CAUSE_TIMER1`=1）。`g_int_handler` が要因テーブルで
`timer0_interrupt_handler` / `timer1_interrupt_handler` に分岐する。

### `g_bios_timer_set`

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_timer.asm`

|          |                                                                                               |
| :------- | :-------------------------------------------------------------------------------------------- |
| **説明** | タイマー番号 0/1 の周期と回数を設定して開始する。`周期=0` で停止、`回数=0` で無限             |
| **入力** | `R0` = タイマー番号（0 または 1）。`R1` = 周期 ms（16bit）。`R2` = 回数（16bit） |
| **出力** | `R0` = OK / NG（番号が 0/1 以外なら IO ボードが NG を返す）                                   |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                              |
| **線上** | 送信 6B: `12h`, timerNo, periodH, periodL, countH, countL → 受信 1B: status                   |
| **作業** | 送信フレーム 6 ワードはスタック上に確保（`_WORK` は使わない）                                 |

> 応答 1 バイトは送信完了（`HSHK_ENA` が 0 に戻る）後、**IO→CPU の別トランザクション**で届く。
> このラッパはハンドシェイク割り込みを待たず `g_hshk_wait_req1_1` で `HSHK_REQ_1` を
> ポーリングし、`g_hshk_accept_request` → `g_hshk_recv_byte` → `g_hshk_finalize_recv`
> で受け取る（同期呼び出し）。

| 入力値     | 意味                             |
| :--------- | :------------------------------- |
| period = 0 | そのタイマーを停止（回数は無視） |
| count = 0  | 停止指示まで無限に割り込む       |
| count > 0  | 指定回数だけ割り込んで自動停止   |

```asm
	ld	R0, TIMER_NO		; 第1引数: タイマー番号
	ld	R1, TIMER_PERIOD	; 第2引数: 周期 ms
	ld	R2, TIMER_COUNT		; 第3引数: 回数
	bald	g_bios_timer_set
```

> 割り込みハンドラ側で `INTERRUPT_BUSY` を立てている間、IO ボードは次の割り込み配送を
> 保留する（要因の取り違え防止）。ハンドラは短く保つこと。

---

## 時刻取得

ハンドシェイク **コマンド `11h`（時刻取得）**。モード不問。  
I/O ボードの 64bit タイマーを、上位バイト（時刻7）から下位（時刻0）の順で返す。

### `g_hshk_get_time`

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_get_time.asm`

|          |                                                                                                              |
| :------- | :----------------------------------------------------------------------------------------------------------- |
| **説明** | IO ボードから 64bit 時刻を取得し、呼び元がスタックに確保した **4 ワード**へ書く                              |
| **入力** | なし（レジスタ引数なし）。呼び元が **4 ワード**（1 ワード 2 バイト、ビッグエンディアン）をスタックに確保する |
| **出力** | `R0` = OK / NG。成功時 `SP+2` … `SP+5`（callee 視点）に (時刻7:6) … (時刻1:0)                                |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                                             |
| **線上** | 送信 1B: `11h` → 受信 9B: time7..time0 + status                                                              |

> 応答は送信完了後の **IO→CPU 別トランザクション**。`g_hshk_wait_req1_1` で
> `HSHK_REQ_1` をポーリングし、`accept` → `recv_byte`×9 → `finalize_recv`（同期）。

```asm
	si	SP, #4			; 4 ワード（64bit）
	bald	g_hshk_get_time
	; 戻り後 SP+1 … SP+4 = (時刻7:6) … (時刻1:0)
	ai	SP, #4			; 確保したスロットを捨てる
```

---

## 未定義命令LED

ハンドシェイク **コマンド `13h`（未定義命令LED）**。モード不問。  
IO ボードの砲弾 B（UNDEF）を点灯／消灯する。INT0（未定義命令）から点灯し、RST 等で消灯する想定。

### `g_bios_undef_led`

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_undef_led.asm`

|          |                                                                                  |
| :------- | :------------------------------------------------------------------------------- |
| **説明** | IO ボードの未定義命令 LED（砲弾 B）を点灯または消灯する                          |
| **入力** | `R0` = Bit0（0=消灯 / 1=点灯。他ビットは無視して Bit0 のみ送る）                 |
| **出力** | `R0` = OK / NG                                                                   |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                 |
| **線上** | 送信 2B: `13h`, flag → 受信 1B: status                                           |

> 応答は送信完了後の **IO→CPU 別トランザクション**。`g_hshk_wait_req1_1` で
> `HSHK_REQ_1` をポーリングし、`accept` → `recv_byte` → `finalize_recv`（同期）。

```asm
	mvi	R0, #1			; 点灯
	bald	g_bios_undef_led
```

---

## 7セグメントLED駆動

ハンドシェイク **コマンド `16h`（LED表示依頼）** の 7セグ部。  
**フリーモード専用**（モニターモードでは NG=`01h`）。ブートモニタ自身は呼ばない想定。  
ビット: `[0..7]` = `[a,b,c,d,e,f,g,dp]`（`seven_segment_bit_map.svg`）。  
桁番号 0–11 = 左から ADDR 8桁 + DATA 4桁。  
ユーザバッファは **1 ワード 1 バイト**（下位 8bit。桁番号＝ワードオフセット）。  
7セグと砲弾は同一トランザクションのため、片方だけ更新する API は `_SYS_PAGE0` 内部ラッチ（`GL_HSHK_LED_LATCH`、`handshake_led.asm` の `.ds 14`）を使う。

### `g_bios_led_init`

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_led.asm`（`g_main` から呼ぶ）

|          |                                                                                                     |
| :------- | :-------------------------------------------------------------------------------------------------- |
| **説明** | `_SYS_PAGE0` の LED 内部ラッチ（`GL_HSHK_LED_LATCH`、7seg×12 + 砲弾×2）を 0 にする。リセット後 1 回 |
| **入力** | なし                                                                                                |
| **出力** | なし                                                                                                |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                                    |

### `g_bios_led_seven_seg`

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_led.asm`

|          |                                                                                                                                      |
| :------- | :----------------------------------------------------------------------------------------------------------------------------------- |
| **説明** | 12 桁分のパターンを送り、砲弾 LED は前回ラッチ（または 0）を維持して `16h` を発行                                                    |
| **入力** | `R0` = パターンバッファ先頭（ワードアドレス）。**12 ワード**（各桁 下位 8bit）。`R1` = 砲弾維持モード（0=両バイト 0 / 1=内部ラッチ） |
| **出力** | `R0` = OK / `01h` モードエラー / `02h` その他                                                                                        |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                                                                     |
| **線上** | 送信 15B: `16h` + 7seg×12 + bullet0_7 + bullet8_F → 受信 1B: status                                                                  |

| バッファ相対 | 内容        |
| :----------: | :---------- |
|   +0 … +7    | ADDR 桁 0–7 |
|   +8 … +11   | DATA 桁 0–3 |

> 砲弾も同時に変えたい場合は `g_bios_led_display`（下記）を使う。

### `g_bios_led_display` — 7セグ＋砲弾一括

|          |                                                                                    |
| :------- | :--------------------------------------------------------------------------------- |
| **説明** | `16h` をそのまま送るフル更新。内部ラッチも更新する                                 |
| **入力** | `R0` = バッファ先頭。**14 ワード**: 7seg×12 + bullet0_7 + bullet8_F（各下位 8bit） |
| **出力** | `R0` = OK / モードエラー / その他                                                  |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                   |
| **線上** | 送信 15B: `16h` + 7seg×12 + bullet0_7 + bullet8_F → 受信 1B: status                |

---

## 砲弾LED駆動

同じ **`16h`** の砲弾部（位置 `0Dh` / `0Eh`）。フリーモード専用。

### `g_bios_led_bullet`

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_led.asm`

|          |                                                                                                        |
| :------- | :----------------------------------------------------------------------------------------------------- |
| **説明** | 砲弾 16 本の ON/OFF を更新。7セグは内部ラッチ（または消灯）を維持して `16h` 発行                       |
| **入力** | `R0` 下位 8bit = 砲弾 0–7（Bit0=LED0 … Bit7=LED7）。`R1` 下位 8bit = 砲弾 8–F（Bit0=LED8 … Bit7=LEDF） |
| **出力** | `R0` = OK / モードエラー / その他                                                                      |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                                                       |
| **線上** | `16h` フルフレーム（7seg はラッチ値）                                                                  |

|     ビット     | LED      |
| :------------: | :------- |
| Bit0–7 of `R0` | 砲弾 0–7 |
| Bit0–7 of `R1` | 砲弾 8–F |

> ADS 表示など IO パネル側の用途と衝突しうる。ユーザープログラムがフリーモードで占有する想定。

---

## キーボード入力（IOボードのキーボード）

ハンドシェイク **コマンド `14h`（16進キー入力取得）**。  
**フリーモード専用**。横キー列の押下ビットマップを返す（16進＋ファンクション相当を列に割当）。

### `g_bios_hex_key_get`

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_hex_keyboard.asm`

|          |                                                                               |
| :------- | :---------------------------------------------------------------------------- |
| **説明** | IO ボード前面キーボードの現在の押下状態を取得（ポーリング）。フリーモード専用 |
| **入力** | `R0` = 結果バッファ先頭（ワードアドレス）。**8 ワード**（各列 下位 8bit）     |
| **出力** | `R0` = OK / `01h` モードエラー / `02h` その他。バッファに列 0–7 を書く        |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する）                                              |
| **線上** | 送信 1B: `14h` → 受信 9B: col0..col7 + status                                 |

| バッファ相対 | 内容                                     |
| :----------: | :--------------------------------------- |
|   +0 … +7    | 列 0–7。各バイトの各 Bit=1 でそのキー ON |
| （応答末尾） | status（`R0` にも同じ値）                |

> ファンクションキー F0–F7 の論理名は `ioboard.mdc`。BIOS は生ビットマップのみ返し、デコードはアプリ側。

---

## キーボード入力（PCのキーボード）

ハンドシェイク **コマンド `15h`（PCキー入力取得）**。  
USB 経由で PC（Cursor / ホスト）から中継された 1 打鍵を取得。モード不問（仕様上 OK/NG のみ）。

### `g_bios_pc_key_get`

実装: `retrocpu_boot_monitor/mn1613/src/handshake/handshake_pc_keyboard.asm`

|          |                                                                                                                                                          |
| :------- | :------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **説明** | PC キー 1 件を取得。未入力時は ASCII/コードとも 0、status=OK もあり得る。モード不問。結果はレジスタで返す（スタックバッファ不要） |
| **入力** | なし（レジスタ引数なし） |
| **出力** | `R0` = OK / NG。`R1` = ASCII（下位 8bit）。`R2` = キーコード（下位 8bit。キーコード表: https://hspnext.sakura.ne.jp/keycode/keycode.html）。失敗時は `R0`=NG、`R1`=`R2`=0 |
| **破壊** | R0, R1, R2（R3–R4 は退避・復元する） |
| **線上** | 送信 1B: `15h` → 受信 3B: ascii, keyCode, status |

> 応答は送信完了後の **IO→CPU 別トランザクション**。`g_hshk_wait_req1_1` で
> `HSHK_REQ_1` をポーリングし、`accept` → `recv_byte`×3 → `finalize_recv`（同期）。

```asm
	bald	g_bios_pc_key_get
	; R0 = status、R1 = ASCII、R2 = キーコード
```

---

## 実装メモ

- 公開エントリは `g_*`（`.global`、`BALD`/`RET`）。データは `GL_*`。コードはセグメント 0。
- `16h` は 7セグと砲弾が **同一トランザクション**。片方だけ更新する BIOS は内部ラッチ必須。
- 線仕様の詳細・ACK リトライは `HandShake.mdc` に従う。
- 引数が 4 つ以上のエントリはスタック渡し。呼び出し規約とスタックの並びは `asm-rules.mdc`。
- 作業変数は `.area _WORK`（各 `.asm` の `.ds`）、値ありテーブルは `.area _DATA`。`_CODE` / `_DATA` は書き換えない。
- `.asm` はインクルードしない。`handshake_common.asm` / `bios_common.asm` などは個別アセンブルし `sdld` で結合する。
